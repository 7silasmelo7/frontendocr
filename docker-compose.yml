

services:
  frontend:
    build: .
    ports:
      - "3000:3000"
    volumes:
      
      - ./:/app      
      - /app/node_modules
      
    
    environment:
      
      - WATCHPACK_POLLING=true
      - CHOKIDAR_USEPOLLING=true
    
    
    command: npm run dev